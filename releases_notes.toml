# DEPRECATED — prefer releases/<tag>.yaml (see releases/README.md).
# Kept only as a historical pointer; .github/scripts/release.py no longer reads this file.
#
# Migration:
#   releases_notes.toml [releases."0.0.5"]  →  releases/v0.0.5.yaml
